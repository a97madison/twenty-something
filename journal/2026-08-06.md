# 2026-08-06

## Committed / merged
- `4aa51f8` #game #web Cap the web app to a phone-width column so cards don't blow up. Expo web export was stretching cards across the full browser width; App.tsx now constrains the app to a phone-width column (10 insertions in `apps/game/src/App.tsx`) so web renders at phone size.
- `336d1f6` #meta #memory Add STATE.md second-brain spine, auto-injected each session by the SessionStart hook.
- `2e08931` #docs Go-viral marketing roadmap (`docs/MARKETING.md`).
- `5f087b9` #game Cream app icon to match the Android adaptive icon + splash.
- `bc3eeeb` #game Clearer rating-gate copy + worked example solutions in how-to-play.

These close out the 2026-06-29 playtest batch (also fc3da80 wrong-answer commit+advance / lenient scoring / red result pill, 0ebe18a calculator white card faces). Working tree back to clean on `main`.

## State
- STATE.md updated: Right now notes the web phone-width cap; In flight records the batch as fully committed with 4aa51f8 as latest.

## Next
1. EAS dev-build milestone (gates App Check, live notifications, store distribution).
2. Wire `submitDaily` streak + `submitRoomSolution` callables to the client, set public `allUsers` invoker.
3. Growth per `docs/MARKETING.md`.
