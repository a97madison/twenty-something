# 2026-08-01

## Committed the 2026-06-29 playtest batch + spine

The working-tree changes that had been sitting uncommitted on `main` are now landed and merged. Highlights from the recent commits:

- **fc3da80 #game #ui** Wrong answers commit + advance the hand, lenient scoring (SLOW_MS 60s to 90s), red result pill on a wrong target. Touches `engine.ts`, `GameScreen.tsx`, `CalcPad.tsx`, with new coverage in `game.test.ts` / `score.test.ts`.
- **bc3eeeb #game** Clearer rating-gate copy plus worked example solutions in how-to-play (`InstructionsScreen.tsx`, `StatsScreen.tsx`). This is the InstructionsScreen change STATE.md had flagged to exclude; it went in cleanly here.
- **0ebe18a #ui #calculator** White card faces in the calculator card row (`CardRow.tsx`).
- **5f087b9 #game** Cream app icon to match the Android adaptive icon + splash (`assets/icon.png`).
- **2e08931 #docs** Go-viral marketing roadmap (`docs/MARKETING.md`, 177 lines).
- **336d1f6 #meta #memory** Added the STATE.md second-brain spine, auto-injected each session by the SessionStart hook.

## State now
- Working tree clean on `main`. Nothing mid-flight.
- Old Next item 1 (commit + merge the batch) is done, so the priority list shifts up: EAS dev-build milestone is now the single next real gate.

## Next
1. EAS dev-build (account-gated: `eas login`, Apple Developer + Play Console). Unblocks App Check, OS notifications, store distribution.
2. Wire `submitDaily` and `submitRoomSolution` callables to the client + set public `allUsers` invoker.
3. Growth per `docs/MARKETING.md`.
